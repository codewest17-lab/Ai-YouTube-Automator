<!--
  Merge these into android/app/src/main/AndroidManifest.xml (generated by
  `npx cap add android`), inside the existing <manifest> and <application>
  tags respectively.
-->

<!-- Inside <manifest ...> -->
<uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
<uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

<!-- Inside <application ...> -->
<service
    android:name="com.ajet.youtubeuploader.FolderWatchService"
    android:foregroundServiceType="dataSync"
    android:exported="false" />

<!--
  MANAGE_EXTERNAL_STORAGE requires a Play Store "All files access"
  declaration if you ever publish this app; for personal sideloaded use
  (installing the APK directly on your own device) no store review is
  needed, only the on-device consent screen the plugin opens.
-->
